<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuarios del Sistema - Agenda TCE</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 0;
        }
        .container {
            max-width: 1200px;
        }
        .header {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
        }
        .header-icon {
            font-size: 3rem;
            color: #667eea;
            margin-bottom: 1rem;
        }
        .header-title {
            font-size: 2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 0.5rem;
        }
        .header-subtitle {
            color: #666;
            font-size: 1.1rem;
        }
        .users-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }
        .user-card {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
            cursor: pointer;
        }
        .user-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }
        .user-card.admin {
            border-top: 5px solid #dc3545;
        }
        .user-card.supervisor {
            border-top: 5px solid #ffc107;
        }
        .user-card.agent {
            border-top: 5px solid #28a745;
        }
        .user-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }
        .user-icon.admin {
            color: #dc3545;
        }
        .user-icon.supervisor {
            color: #ffc107;
        }
        .user-icon.agent {
            color: #28a745;
        }
        .user-role {
            font-size: 0.9rem;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 1rem;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            display: inline-block;
        }
        .user-role.admin {
            background: #dc3545;
            color: white;
        }
        .user-role.supervisor {
            background: #ffc107;
            color: #333;
        }
        .user-role.agent {
            background: #28a745;
            color: white;
        }
        .user-name {
            font-size: 1.3rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
        }
        .user-id {
            font-size: 0.9rem;
            color: #666;
            font-family: monospace;
            background: #f8f9fa;
            padding: 0.5rem;
            border-radius: 5px;
            margin-bottom: 1rem;
        }
        .user-permissions {
            margin-bottom: 1.5rem;
        }
        .permission-item {
            display: flex;
            align-items: center;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }
        .permission-item i {
            margin-right: 0.5rem;
            width: 16px;
        }
        .login-btn {
            width: 100%;
            padding: 0.8rem;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .login-btn.admin {
            background: #dc3545;
            color: white;
        }
        .login-btn.admin:hover {
            background: #c82333;
            color: white;
        }
        .login-btn.supervisor {
            background: #ffc107;
            color: #333;
        }
        .login-btn.supervisor:hover {
            background: #e0a800;
            color: #333;
        }
        .login-btn.agent {
            background: #28a745;
            color: white;
        }
        .login-btn.agent:hover {
            background: #218838;
            color: white;
        }
        .system-info {
            background: rgba(255,255,255,0.1);
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 2rem;
            color: white;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="header-icon">
                <i class="fas fa-users"></i>
            </div>
            <h1 class="header-title">Usuarios del Sistema</h1>
            <p class="header-subtitle">Sistema de Agenda TCE - Selecciona un usuario para iniciar sesión</p>
        </div>

        <div class="system-info">
            <i class="fas fa-info-circle me-2"></i>
            <strong>Sistema de Prueba:</strong> Estos usuarios están configurados para simular diferentes roles y permisos en el sistema de agenda.
        </div>

        <div class="users-grid">
            <!-- Usuario Administrador -->
            <div class="user-card admin" onclick="loginUser('682e2e8d8be24eb085a7fcc5', 'admin')">
                <div class="text-center">
                    <div class="user-icon admin">
                        <i class="fas fa-crown"></i>
                    </div>
                    <div class="user-role admin">Administrador</div>
                    <h3 class="user-name">Alejandro Botero</h3>
                    <div class="user-id">ID: </div>
                </div>
                
                <div class="user-permissions">
                    <h6><i class="fas fa-key me-1"></i>Permisos:</h6>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Acceso completo al sistema
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Configuración de usuarios y tareas
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Visualización de historial completo
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Gestión de departamentos
                    </div>
                </div>
                
                <button class="login-btn admin">
                    <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión como Admin
                </button>
            </div>

            <!-- Usuario Supervisor -->
            <div class="user-card supervisor" onclick="loginUser('673a97324cec319c00c284e1', 'supervisor')">
                <div class="text-center">
                    <div class="user-icon supervisor">
                        <i class="fas fa-user-tie"></i>
                    </div>
                    <div class="user-role supervisor">Supervisor</div>
                    <h3 class="user-name">Yorman</h3>
                    <div class="user-id">ID: </div>
                </div>
                
                <div class="user-permissions">
                    <h6><i class="fas fa-key me-1"></i>Permisos:</h6>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Gestión de tareas del equipo
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Visualización de historial
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-warning"></i>
                        Acceso limitado a configuración
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Reportes y estadísticas
                    </div>
                </div>
                
                <button class="login-btn supervisor">
                    <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión como Supervisor
                </button>
            </div>

            <!-- Usuario Agente -->
            <div class="user-card agent" onclick="loginUser('67680d4862732ee82e054a57', 'agent')">
                <div class="text-center">
                    <div class="user-icon agent">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="user-role agent">Agente</div>
                    <h3 class="user-name">Bernstein</h3>
                    <div class="user-id">ID: </div>
                </div>
                
                <div class="user-permissions">
                    <h6><i class="fas fa-key me-1"></i>Permisos:</h6>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Gestión de tareas asignadas
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-check text-success"></i>
                        Subida de evidencias
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-times text-danger"></i>
                        Sin acceso a configuración
                    </div>
                    <div class="permission-item">
                        <i class="fas fa-times text-danger"></i>
                        Sin acceso a historial completo
                    </div>
                </div>
                
                <button class="login-btn agent">
                    <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión como Agente
                </button>
            </div>
        </div>

        <div class="text-center mt-4">
            <small class="text-white">
                <i class="fas fa-shield-alt me-1"></i>
                Sistema de autenticación simulado para pruebas de desarrollo
            </small>
        </div>
    </div>

    <script>
        function loginUser(userId, role) {
            // Mostrar loading en la tarjeta seleccionada
            const card = event.currentTarget;
            const button = card.querySelector('.login-btn');
            const originalButtonText = button.innerHTML;
            
            button.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Iniciando sesión...';
            button.disabled = true;
            
            // Simular delay de login
            setTimeout(() => {
                // Crear formulario para enviar datos
                const form = document.createElement('form');
                form.method = 'POST';
                form.action = '/login-simulado';
                
                const userIdInput = document.createElement('input');
                userIdInput.type = 'hidden';
                userIdInput.name = 'userId';
                userIdInput.value = userId;
                
                const roleInput = document.createElement('input');
                roleInput.type = 'hidden';
                roleInput.name = 'role';
                roleInput.value = role;
                
                form.appendChild(userIdInput);
                form.appendChild(roleInput);
                document.body.appendChild(form);
                form.submit();
            }, 1500);
        }
    </script>
</body>
</html>
